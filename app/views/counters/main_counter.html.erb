<script>
  function loadXMLDoc()
  {


    var xmlhttp;
    if (window.XMLHttpRequest)
    {// code for IE7+, Firefox, Chrome, Opera, Safari
      xmlhttp=new XMLHttpRequest();
    }
    else
    {// code for IE6, IE5
      xmlhttp=new ActiveXObject("Microsoft.XMLHTTP");
    }
    xmlhttp.onreadystatechange=function()
    {
      if (xmlhttp.readyState==4 && xmlhttp.status==200)
      {
        document.getElementById("myDiv").innerHTML=xmlhttp.responseText;
      }
    }
    
    xmlhttp.open("GET","/swipeCards/card_details?id="+$('#card_no').val(),true);
    xmlhttp.send();
   
  }

  function card_no(order)
  {

  order=$('#card_no').val()
    //alert(order);

    $.ajax({
      type: "GET",
      url: "/swipeCards/card_session",

      data: {
        'id' : order
      }

    });
//loadXMLDoc();
  }
 function cancel()
  {

  var xmlhttp;
    if (window.XMLHttpRequest)
    {// code for IE7+, Firefox, Chrome, Opera, Safari
      xmlhttp=new XMLHttpRequest();
    }
    else
    {// code for IE6, IE5
      xmlhttp=new ActiveXObject("Microsoft.XMLHTTP");
    }
    xmlhttp.onreadystatechange=function()
    {
      if (xmlhttp.readyState==4 && xmlhttp.status==200)
      {
        document.getElementById("myDiv").innerHTML=xmlhttp.responseText;
      }
    }

    xmlhttp.open("GET","/swipeCards/cancel",true);
    xmlhttp.send();
    

   
//loadXMLDoc();
  }

</script>
<style>
  #myDiv1{
    width: 300px;
    height: 400px;
    border:1px solid;
    margin-top: 50px;
    margin-left: 20px;
    
    
}
#myDiv{
  width: 300px;
    height: 375px;
    background-color:whitesmoke;
}
</style>
<div>
  CARD NUMBER:<%=text_field_tag "card_no"%> 
  <button type="button" class="btn-" onclick="loadXMLDoc()">Ok</button>

<br/><br/><br/>
<%=link_to_modal 'New Swipe Card', '/swipeCards/new', :class=>"btn-large btn-success"%>
<%=link_to_modal 'Recharge', '/swipeCards/recharge', :class=>"btn-large btn-primary"%>
<%=link_to_modal 'Refund', '/swipeCards/refund', :class=>"btn-large btn-warning"%>
</div>
<div id="myDiv1">
  CARD DETAILS:
<div id="myDiv">

</div>
</div>
